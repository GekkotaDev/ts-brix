# ts-brix

Minimal Typescript runtime utility library.

`ts-brix` provides barrel exports for utilities from `ts-pattern`, `ts-results` and `unionize` along with its own small utilities. It does not aim to be a proper functional programming package like `effect-ts`.
